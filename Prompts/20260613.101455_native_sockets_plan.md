# User Prompt
```
Error: (lazbuild) Broken dependency: indylaz
```

# LAN Broadcaster Native Sockets Implementation Plan

We will rewrite the networking components of the "LAN Broadcaster" application to use Free Pascal's native `sockets` and `ssockets` units instead of Indy. This removes all external package dependencies and ensures compiling out of the box with standard Lazarus / FPC installations.

## User Review Required

> [!IMPORTANT]
> The project dependency on `indylaz` will be removed from [LANBroadcaster.lpi](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpi) and [LANBroadcaster.lpr](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpr).
> All networking logic will rely on standard WinSock / Free Pascal native sockets.

## Proposed Changes

### [MODIFY] [LANBroadcaster.lpr](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpr)
Remove `indylaz` from the `uses` clause.

### [MODIFY] [LANBroadcaster.lpi](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpi)
Remove `<Item> <PackageName Value="indylaz"/> </Item>` from the `RequiredPackages` section.

### [MODIFY] [DiscoveryManager.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/DiscoveryManager.pas)
- Rewrite using native sockets API (specifically low-level socket functions from `sockets` unit for UDP binding, broadcast transmission, and receiving).
- Maintain the exact same public interface and event handlers.

### [MODIFY] [MessageManager.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MessageManager.pas)
- Use FPC `ssockets` (`TInetServer` and `TInetSocket`) or low-level FPC sockets to accept TCP messaging streams on port 5001.
- Maintain the exact same interface and events.

### [MODIFY] [FileTransferManager.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/FileTransferManager.pas)
- Use FPC `ssockets` or low-level sockets to listen/accept and stream files on port 5002.
- Maintain chunked file reads/writes (64KB buffer) to support 1GB+ files.

---

## Verification Plan

### Manual Verification
1. Run `lazbuild` to confirm no package errors.
2. Run two instances, check that automatic discovery works over UDP.
3. Broadcast messages and files to verify TCP transfer functionality.
