# User Prompt
```
'Minimize on Close' can be implemented by referring to another project at 'X:\Projects_X\0_Active\4_Lazarus\AI_Helped\Reminder_Lazarus_GIT'
```

# LAN Broadcaster Minimize on Close Refinement Plan

We will align the "Minimize on Close" behavior of `LAN_Broadcaster_Lazarus_GIT` with the robust implementation found in `Reminder_Lazarus_GIT`. Specifically, we will ensure that `FormCloseQuery` explicitly handles all pathways (including `Application.Terminated` and Windows end session scenarios) to avoid getting stuck or blocking system shutdown, and ensure `CanClose` is set explicitly in all branches.

## User Review Required

> [!IMPORTANT]
> We will update `FormCloseQuery` in [MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas) to:
> - Explicitly allow closing if `FClosingFromTray` is `True` or `Application.Terminated` is `True`.
> - Explicitly set `CanClose := True` if `CheckBoxMinimizeClose.Checked` is `False`.
> - Keep the dynamic tray balloon notification logic.

## Proposed Changes

### [MainForm]

#### [MODIFY] [MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas)

We will modify `TFormMain.FormCloseQuery` to match the structured logic from `Reminder_Lazarus_GIT`:

```pascal
procedure TFormMain.FormCloseQuery(Sender: TObject; var CanClose: Boolean);
begin
  if FClosingFromTray or Application.Terminated then
    CanClose := True
  else
  begin
    if CheckBoxMinimizeClose.Checked then
    begin
      CanClose := False;
      Hide;
      TrayIcon.Visible := True;
      { Setup tray properties dynamically at runtime via TypInfo to prevent version mismatches }
      SetTrayBalloonProperties(TrayIcon, 'LAN Broadcaster', 'Application is minimized to the system tray.');
      { Show a subtle balloon hint only once to notify user it is in the tray }
      TrayIcon.ShowBalloonHint;
    end
    else
      CanClose := True;
  end;
end;
```

---

## Verification Plan

### Automated Tests
- Build verification using `C:\lazarus\lazbuild.exe` via command tool to ensure compile correctness.

### Manual Verification
1. Run the rebuilt application.
2. Uncheck "Minimize on Close" and click the [X] button. Confirm it exits immediately.
3. Check "Minimize on Close", click the [X] button. Confirm it minimizes to tray and shows the balloon hint.
4. Restore and exit the application via Tray Icon context menu (Exit). Confirm it exits properly.
