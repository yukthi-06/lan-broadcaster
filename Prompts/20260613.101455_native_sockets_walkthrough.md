# User Prompt
```
Error: (lazbuild) Broken dependency: indylaz
```

# LAN Broadcaster Native Sockets Walkthrough

We have successfully migrated the network layer of the LAN Broadcaster application to use native Free Pascal / Windows WinSock2 socket units. This removes all dependencies on Indy (`indylaz`), ensuring compiling out-of-the-box on clean Lazarus / FPC installations.

## Changes Made

1. **[LANBroadcaster.lpr](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpr)**: Removed `indylaz` package reference from the program's `uses` clause.
2. **[LANBroadcaster.lpi](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpi)**: Removed `indylaz` from the project XML required packages definition.
3. **[Utilities.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/Utilities.pas)**: Replaced `IdStack` with WinSock2 API structure checks (`WSAStartup`, `GetHostName`, `GetHostByName`, `inet_ntoa`) for obtaining the local machine IPv4 address.
4. **[DiscoveryManager.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/DiscoveryManager.pas)**: Replaced `TIdUDPServer` and `TIdUDPClient` with low-level WinSock `socket`, `bind`, `recvfrom`, and `sendto` calls inside standard TThread worker structures. Handles multi-instance reuse and heartbeat packets safely.
5. **[MessageManager.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MessageManager.pas)**: Replaced `TIdTCPServer` and `TIdTCPClient` with native Windows TCP streaming. Spawns incoming connection handlers dynamically in the background to handle MSG packets.
6. **[FileTransferManager.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/FileTransferManager.pas)**: Replaced Indy TCP file transfer with native sockets chunked stream. Implements RecvLine/SendLine custom protocol utilities to handle metadata and ACK handshakes, streaming 64KB buffers to avoid loading large files into memory.

## Next Steps
1. Re-open [LANBroadcaster.lpi](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/LANBroadcaster.lpi) in your Lazarus IDE.
2. Press **Ctrl + F9** to compile or **F9** to run. The project should now build immediately without broken package errors.
