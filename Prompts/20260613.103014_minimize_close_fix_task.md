# User Prompt
```
Clicking on close actually exits app even if 'Minimize on Close' option is set
```

# LAN Broadcaster Minimize on Close Fix Task List

- [x] Add `OnCloseQuery = FormCloseQuery` property to `FormMain` object in `MainForm.lfm`
- [ ] Verify build and minimize-on-close behavior (delegated to user IDE build)
