# User Prompt
```
error reading TrayIcon.BaloonText unknown property
```

# LAN Broadcaster Balloon Hint Properties Fix Plan

We will resolve the form reading error caused by Lazarus LCL version mismatches in spelling the balloon properties (`BaloonText`/`BalloonText`). We will remove the serialized balloon properties from the `.lfm` and invoke `ShowBalloonHint` dynamically with parameter arguments in code.

## User Review Required

> [!IMPORTANT]
> The properties `BalloonTitle`, `BalloonText`, and `BalloonFlags` will be removed from the form file [MainForm.lfm](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.lfm).
> The balloon notification will be triggered programmatically in [MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas) via `TrayIcon.ShowBalloonHint('LAN Broadcaster', 'Application is minimized to the system tray.', bfInfo, 3000)`.

## Proposed Changes

### [MODIFY] [MainForm.lfm](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.lfm)
Remove `BalloonTitle`, `BalloonText`, and `BalloonFlags` lines from the `TrayIcon` object definition.

### [MODIFY] [MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas)
Update `TrayIcon.ShowBalloonHint;` to call the parameterized version: `TrayIcon.ShowBalloonHint('LAN Broadcaster', 'Application is minimized to the system tray.', bfInfo, 3000);` inside `FormCloseQuery`.

---

## Verification Plan

### Manual Verification
1. Re-open project in Lazarus, verify the form loads without property reading errors.
2. Minimize to tray by closing the form (with Minimize on Close checked) and verify that the balloon popup appears.
