# User Prompt
```
Application should have settings to 
* Start on Windows Startup
* Start Minimized
* Minimize On Close
* Enable Notification Sounds
```

# LAN Broadcaster Settings Walkthrough

We have successfully integrated the requested configuration settings panel into the main LCL Form, along with system-level hooks for registry startup control, tray-minimization routines, and sound alerts.

## Changes Made

1. **[MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas)**:
   - Added `TIniFile` persistence (`settings.ini` alongside executable).
   - Added Windows Registry modification routines (`SetStartupRegistry` / `GetStartupRegistry`) under `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`.
   - Intercepted form closing in `FormCloseQuery` to minimize/hide to system tray if the option is active.
   - Checked `Start Minimized` inside `FormShow` to boot directly into system tray mode.
   - Added audio alerts with `PlaySound` from `mmsystem` unit for new chat messages, file arrivals, and online/offline notifications.
2. **[MainForm.lfm](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.lfm)**:
   - Added a "Settings" `TGroupBox` control at the bottom of the left sidebar containing the four setting check boxes.
   - Added `TTrayIcon` and a corresponding `TPopupMenu` with "Restore" and "Exit" actions to support minimized tray operations.

## Next Steps
1. Compile and run the project inside the Lazarus IDE.
2. Try toggling each setting in the bottom-left pane and verify functionality!
