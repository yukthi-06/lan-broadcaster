# User Prompt
```
error reading TrayIcon.BaloonText unknown property
```

# LAN Broadcaster Balloon Hint Properties Fix Walkthrough

We have successfully resolved the form loading error regarding missing design-time tray properties by moving the balloon hint setup to programmatic execution.

## Changes Made

1. **[MainForm.lfm](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.lfm)**:
   - Removed design-time properties `BalloonTitle`, `BalloonText`, and `BalloonFlags` from the `TrayIcon` object definition.
2. **[MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas)**:
   - Updated `TrayIcon.ShowBalloonHint;` to the parameterized version: `TrayIcon.ShowBalloonHint('LAN Broadcaster', 'Application is minimized to the system tray.', bfInfo, 3000);` in `FormCloseQuery`.

## Next Steps
1. Recompile and run the project inside the Lazarus IDE.
2. Verify that the form loads cleanly and minimizes to the system tray without issues.
