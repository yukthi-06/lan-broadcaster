# User Prompt
```
Clicking on close actually exits app even if 'Minimize on Close' option is set
```

# LAN Broadcaster Minimize on Close Fix Walkthrough

We have successfully resolved the issue where closing the window exited the application instead of minimizing to the tray by mapping the form's close query event handler.

## Changes Made

1. **[MainForm.lfm](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.lfm)**:
   - Added the property registration `OnCloseQuery = FormCloseQuery` to link the main form's close-query event to the existing handler method inside `MainForm.pas`.

## Next Steps
1. Recompile and run the project inside the Lazarus IDE.
2. Confirm that with "Minimize on Close" selected, clicking the window close [X] button hides the form and moves it to the tray, and that selecting Restore from the tray icon popup menu returns it to normal state.
