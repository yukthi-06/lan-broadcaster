# User Prompt
```
X:\Projects_X\0_Active\4_Lazarus\AI_Helped\LAN_Broadcaster_Lazarus_GIT\MainForm.pas(168,110) Error: (3026) Wrong number of parameters specified for call to "ShowBalloonHint"
C:\lazarus\lcl\units\x86_64-win64\extctrls.ppu:customtrayicon.inc(268,27) Error: (5088) Found declaration: ShowBalloonHint;
```

# LAN Broadcaster TypInfo Balloon Hint Plan

We will resolve the compilation error by calling the parameterless `ShowBalloonHint` method, which is the only version supported by this LCL version. To set the spelling-sensitive balloon properties (`BalloonText`/`BaloonText`) in a cross-version compatible way, we will use runtime reflection (`TypInfo` unit) to inspect and assign properties at runtime.

## User Review Required

> [!IMPORTANT]
> - The application will use dynamic RTTI (`TypInfo`) to set tray icon properties at runtime to prevent compile-time spelling version conflicts.
> - `ShowBalloonHint` will be called as a parameterless method, matching the LCL declaration.

## Proposed Changes

### [MODIFY] [MainForm.pas](file:///x:/Projects_X/0_Active/4_Lazarus/AI_Helped/LAN_Broadcaster_Lazarus_GIT/MainForm.pas)
- Add `TypInfo` to the implementation `uses` clause.
- Implement a helper procedure `SetTrayBalloonProperties(Tray: TTrayIcon; const ATitle, AText: string)` to inspect if `BalloonText` or `BaloonText` exists and assign it.
- Modify `FormCloseQuery` to call `SetTrayBalloonProperties` and then invoke `TrayIcon.ShowBalloonHint;` without parameters.

---

## Verification Plan

### Manual Verification
1. Rebuild the application in Lazarus and verify it compiles without parameter count or missing property errors.
2. Minimize to tray and verify the balloon hint pops up correctly.
