# User Prompt
```
error reading TrayIcon.BaloonText unknown property
```

# LAN Broadcaster Balloon Hint Properties Fix Task List

- [x] Remove `BalloonTitle`, `BalloonText`, and `BalloonFlags` from `MainForm.lfm`
- [x] Update `FormCloseQuery` in `MainForm.pas` to use parameterized `ShowBalloonHint` call
- [ ] Verify build (delegated to user IDE build)
